# =============================================================================
# BASE - Environment Variables Configuration
# =============================================================================
# Copy this file to .env and adjust the values according to your needs.
# cp .env.example .env
#
# Note: This file supports profile-based configuration:
# - .env.shared (shared across all profiles)
# - .env (default profile)
# - .env.{PROFILE} (profile-specific, e.g., .env.development, .env.production)
# =============================================================================

# =============================================================================
# PYSLOG - Structured Logging Configuration
# =============================================================================
# Configure structured logging using structlog

# Log level: CRITICAL, ERROR, WARNING, INFO, DEBUG
# Default: DEBUG
LOGGING_LEVEL=DEBUG

# Output format: "console" (human-readable) or "json" (machine-readable)
# Default: console
LOGGING_FORMAT=console

# Output destination: "stdout" (standard output) or "file" (log file)
# Default: stdout
LOGGING_OUTPUT=stdout

# Log file path (only used when LOGGING_OUTPUT=file)
# Default: app.log
LOGGING_FILE_PATH=app.log

# Include location information (filename, line number, module) in logs
# Options: true, false, 1, 0, yes, no
# Default: false
LOGGING_INCLUDE_LOCATION=false
